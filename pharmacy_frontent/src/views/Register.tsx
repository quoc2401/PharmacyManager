import React, { FC } from "react";
import { useTitle } from "../hooks/useTitle";

const Register: FC = () => {
  useTitle("Pharmacy - Register");

  return <div>Register</div>;
};

export default Register;
